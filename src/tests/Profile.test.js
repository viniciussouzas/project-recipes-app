import React from 'react';
import renderWithRouter from '../helpers/renderWithRouter';
import Profile from '../pages/Profile';

describe('Testa página Profile', () => {
  test('', () => { renderWithRouter(<Profile />); });
});
