import React from 'react';
import renderWithRouter from '../helpers/renderWithRouter';
import FavoriteRecipes from '../pages/FavoriteRecipes';

describe('Testa página FavoriteRecipes', () => {
  test('', () => { renderWithRouter(<FavoriteRecipes />); });
});
